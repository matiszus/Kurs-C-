#include<iostream>
using namespace std;
int main()
{
    int liczba;
    cout << "podaj liczbe:";
    cin >> liczba;
    if (liczba > 10 && liczba < 50)
    {
        cout << "liczba mieści się w zakresie";
    }
    else
    {
        cout << "liczba nie mieści się w przedziale";
    }
}
