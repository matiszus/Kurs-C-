#include<iostream>

using namespace std;

int main()
{

	float dochod;
	cout << "podaj dochod:  ";
	cin >> dochod;
	if (dochod <= 30000) {
		cout << "twoj podatek wynosi: "<<(dochod * 0.12);
	}
	
	
	else {
		cout << "twoj podatek wynosi: " << 30000 * 0.12 + (dochod - 30000) * 0.32;
	}
		
}
